
---
  KeystoneBasic.get_entities:
   -
      runner:
        type: "constant"
        times: {{ compute * 2 }}
        concurrency: {{ concurrency }}
      sla:
        failure_rate:
          max: 0

