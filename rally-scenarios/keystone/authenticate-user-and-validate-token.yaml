---
  KeystoneBasic.authenticate_user_and_validate_token:
    -
      args: {}
      runner:
        type: "constant"
        times: {{ concurrency * 4 * 10 }}
        concurrency: {{ concurrency * 4 }}
      sla:
        failure_rate:
          max: 0
