---
  Authenticate.keystone:
    -
      runner:
        type: "constant"
        times: {{ concurrency * 4 * 1000 }}
        concurrency: {{ concurrency * 4 }}
      context:
        users:
          tenants: 3
          users_per_tenant: 50
      sla:
        failure_rate:
          max: 0
