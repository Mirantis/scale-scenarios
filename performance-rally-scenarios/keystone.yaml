---
  Authenticate.keystone:
    -
      runner:
        type: "rps"
        times: {{ concurrency / 2 * 1000 }}
        rps: {{ concurrency / 2 }}
      context:
        users:
          tenants: 10
          users_per_tenant: 10
      sla:
        failure_rate:
          max: 0
