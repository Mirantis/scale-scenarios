---
  KeystoneBasic.create_delete_user:
    -
      runner:
        type: "constant_for_duration"
        duration: {{ duration }}
        concurrency: {{ concurrency }}
      sla:
        failure_rate:
          max: 0
