---
  Authenticate.keystone:
    -
      runner:
        type: "constant"
        times: {{ concurrency * 2 * 1000 }}
        concurrency: {{ load * 4 }}
      context:
        users:
          tenants: 3
          users_per_tenant: 50
      sla:
        failure_rate:
          max: 0
