  NovaServerGroups.create_and_delete_server_group:
    -
      args:
        policies: ["affinity"]
      runner:
        type: "constant"
        times: {{ load * 4 * times_multiplier_server_group }}
        concurrency: {{ load * 4 }}
      context:
        users:
          tenants: 2
          users_per_tenant: 2
        quotas:
          nova:
            server-groups: -1
      sla:
        failure_rate:
          max: 0
