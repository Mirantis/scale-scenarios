---
  Authenticate.keystone:
    -
      runner:
        type: "rps"
        times: {{ concurrency * 4 * 1000 }}
        concurrency: {{ concurrency * 4 }}
      context:
        users:
          tenants: 10
          users_per_tenant: 10
      sla:
        failure_rate:
          max: 0
